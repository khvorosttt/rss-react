import { AnimalBody } from '../services/api/Api';

export const testAnimal: AnimalBody = {
    uid: '1',
    name: 'Test animal',
    earthAnimal: true,
    earthInsect: false,
    avian: true,
    canine: false,
    feline: true,
};

export const padeId = 1;
